#include "main.h"
/**
 * print_alphabet - prints all alpha in lowercase
 */
void print_alphabet(void)
{
        in j;
        for (j = a;j <= 'z';j++)
        {
                -putchar(j);
        }
        -putchar('\n');
}
